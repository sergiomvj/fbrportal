import { IntakeWizard } from '../_components/IntakeWizard';

export const metadata = {
  title: 'Nova Estrategia | FBR-MKT',
  description: 'Crie uma nova estrategia de marketing',
};

export default function MktNovoPage() {
  return <IntakeWizard />;
}
