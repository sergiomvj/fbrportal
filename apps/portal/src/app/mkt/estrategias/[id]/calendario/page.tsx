import { CalendarGrid } from '../../../_components/CalendarGrid';

export const metadata = {
  title: 'Calendario 90 Dias | FBR-MKT',
};

export default function CalendarioPage() {
  return <CalendarGrid />;
}
