# PRD Shards

AIOS `core-config.yaml` expects sharded PRD content under `docs/prd`.

The current source-of-truth PRD corpus is stored under `fbr-portal-docs`; see `docs/prd.md` and `docs/stories/README.md` for the active source mapping.
