# Architecture Shards

AIOS `core-config.yaml` expects architecture shards under `docs/architecture`.

The active technical source corpus is `fbr-portal-docs` plus the implemented workspace structure in `apps/` and `packages/`.
