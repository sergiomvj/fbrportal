# Padroes de Codigo

Use the conventions established by Story 1.1:

- TypeScript-first packages under `packages/*`.
- Next.js app code under `apps/portal`.
- Shared UI exports through `packages/ui`.
- Environment values documented through `.env.example` files without real secrets.
- Tests must cover package behavior and app integration where applicable.
