# Story 1.4.3: Finance Receivables, Dashboard KPIs, and Sales Payment Intake

## Status

Ready

## Executor Assignment

executor: "aiox-master"
quality_gate: "@architect"
quality_gate_tools:
- story-draft-checklist
- api-contract-review
- lint
- typecheck
- test

## Story

**As a** finance manager,
**I want** receivables APIs, dashboard metrics, and Sales payment intake,
**so that** incoming revenue is visible, filterable, and automatically prepared for reconciliation.

## Acceptance Criteria

1. `GET /api/proxy/finance/recebimentos` supports filters for parceiro, status, date range, company, pagination, and company isolation.
2. `POST /api/proxy/finance/recebimentos` validates required fields, positive monetary values, valid company, and returns descriptive 400/422 errors.
3. `PATCH /api/proxy/finance/recebimentos/{id}/conciliar` marks divergence <= 5% as received, divergence > 5% as divergent, and already reconciled items return 409.
4. Dashboard API returns `receita_total`, `a_receber`, `atrasados`, `divergencias`, `projecao_30d`, previous-period delta, revenue by partner, and revenue by company.
5. Sales `payment.received` intake creates pending receivables and can trigger automatic reconciliation.
6. Frontend includes dashboard KPI cards and a receivables table with filters, create modal, validation, badges, sorting, pagination, loading, retry, and empty states.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] Implement receivables proxy contracts. (AC: 1, 2, 3)
  - [ ] Add `GET`, `POST`, and `PATCH .../{id}/conciliar` portal proxy routes.
  - [ ] Enforce company isolation and validation error semantics.
  - [ ] Implement reconciliation divergence thresholds and 409 conflict behavior.
- [ ] Implement dashboard KPI contract. (AC: 4)
  - [ ] Calculate total revenue, receivables, overdue items, divergences, 30-day projection, previous-period delta, revenue by partner, and revenue by company.
  - [ ] Return stable response shapes for loading/error/empty UI states.
- [ ] Implement Sales payment intake. (AC: 5)
  - [x] Record the Sales `payment.received` intake contract decision.
  - [ ] Accept only the documented `payment.received` payload envelope and fields from the cross-module contract.
  - [ ] Validate required fields from the documented payload and use the standard API error envelope/codes.
  - [ ] Create pending receivables and call automatic reconciliation when eligible.
- [ ] Build dashboard and receivables UI. (AC: 6)
  - [ ] Add KPI cards using shared DataCard-like semantics.
  - [ ] Add receivables table with debounced partner search, status multi-select, date range, pagination, create modal, validation, badges, sorting, and empty state.
- [ ] Add focused tests. (AC: 1-6)
  - [ ] Cover filters, pagination, validation errors, company isolation, reconciliation thresholds, dashboard calculations, Sales event intake, KPI rendering, and receivables table flows.
  - [ ] Run `npm run lint`, `npm run typecheck`, and `npm test`.

## Dev Notes

### Source Context

- Receivables routes are specified in `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#task-fi02-api-routes-de-recebimentos-get-post-patch-conciliar`.
- Dashboard KPIs are specified in `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#task-fi03-api-route-de-dashboard-com-5-kpis-calculados`.
- Dashboard UI cards and receivables table requirements are specified in TASK-FI04 and TASK-FI05.
- Sales `payment.received` event carries partner id/name, company id, amount, currency, reference period, receipt date, partnership type, optional receipt path, and notes. [Source: `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md#113-evento-paymentreceived-fbr-sales-fbr-finance`]

### API and Component Notes

- Required proxy convention is `/api/proxy/{modulo}/{entidade}[/{id}[/{acao}]]`, with `X-User-Id` injected by the Next proxy and `X-Module-Source` identifying module origin. [Source: `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md#12-padroes-de-api-routes`]
- `recebimentos` status values in the SPEC are `pendente`, `recebido`, `atrasado`, and `divergente`.
- Dashboard KPIs use DataCard-like semantics with icon, label, value, delta, trend, loading, error/retry, and finance accent color.
- Cross-module events are REST `POST` requests with typed JSON payload plus `X-User-Id` and `X-Module-Source`; modules must not write directly to another module database. [Source: `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md#11-contratos-cross-module-payloads-detalhados`]

### Architecture/Contract Decision Recorded (2026-05-05)

**Decision owner:** `aiox-master`

1. **Route:** `/api/proxy/finance/recebimentos/sales-intake`
   **File path:** `apps/portal/src/app/api/proxy/finance/recebimentos/sales-intake/route.ts`
2. **Method:** `POST`
   **Headers:** `X-User-Id`, `X-Module-Source: fbr-sales`
3. **Payload:**
   ```json
   {
     "event": "payment.received",
     "data": {
       "parceiro_id": "uuid",
       "parceiro_nome": "string",
       "empresa_id": "uuid",
       "valor": 0.00,
       "moeda": "BRL",
       "periodo_ref": "string (YYYY-MM)",
       "data_recebimento": "string (ISO8601)",
       "tipo_parceria": "string",
       "comprovante_path": "string?",
       "notas": "string?"
     }
   }
   ```
4. **Idempotency:** Checked using `parceiro_id` + `periodo_ref` + `data_recebimento`.
   - Result exists: return 200 (Success, no change).
   - Result new: return 201 (Created).
5. **Reconciliation Trigger:** Invokes the reconciliation service to match against `finance_bank_statements` within a 48h window.
6. **Foundation:** Uses `finance_receivables` and `finance_audit_log` from Story 1.4.2.

### File Locations

- Suggested proxy routes: `apps/portal/src/app/api/proxy/finance/recebimentos/**/route.ts` and `apps/portal/src/app/api/proxy/finance/dashboard/route.ts`
- Suggested UI routes/components: `apps/portal/src/app/finance/page.tsx`, `apps/portal/src/app/finance/recebimentos/page.tsx`, and `apps/portal/src/app/finance/_components/**`
- Suggested shared Finance helpers: `apps/portal/src/lib/finance/{types,schemas,store,fixtures}.ts`

### Dependencies

- Story 1.4.2 is implemented (migrations created).

### Testing

- Use Vitest and Testing Library for API helpers and UI components. Add integration-style route tests following existing portal patterns.
- Sales intake tests: valid documented payload creates a `pendente` receivable, invalid/missing documented fields return the standard 400/422 error envelope, invalid company is rejected through company isolation, and automatic reconciliation is invoked only under the recorded eligibility rule.
- Required final gates: `npm run lint`, `npm run typecheck`, and `npm test`.

## PO Validation Result (2026-05-05)

**Final Assessment: GO.**
- Intake contract defined: YES.
- Dependency 1.4.2 ready: YES.
- Readiness: 10/10.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-03 | 0.1 | Split from parent Story 1.4 Finance backbone scope | @sm |
| 2026-05-05 | 1.0 | Unblocked by aiox-master; intake contract recorded | aiox-master |

## Dev Agent Record

### Agent Model Used

Codex GPT-5

### Completion Notes List

- Recorded Sales intake contract.
- Set story to Ready.

### File List

- docs/stories/1.4.3.finance-receivables-dashboard-sales-events.md
