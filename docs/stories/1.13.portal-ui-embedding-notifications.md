# Story 1.13: Portal UI Module Embedding and Notifications

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
- story-draft-checklist
- lint
- typecheck
- test
- security-review

## Story

**As a** FBR Portal operator,
**I want** secure module iframe embedding, module routing, role guards, realtime notifications, and notification UI,
**so that** authenticated users can access allowed modules and receive cross-module updates safely.

## Acceptance Criteria

1. `ModuleFrame` renders sandboxed iframe, loading skeleton, 10s timeout error, retry, `MODULE_READY -> AUTH_TOKEN`, and `NAVIGATION` URL sync. [Source: `fbr-portal-docs/01_PORTAL_UI/TASKLIST.md#TASK-P14`]
2. Nine module routes render `ModuleFrame` with role guard, unauthorized screen, `/` default redirect by role, fail-closed DB access errors, and middleware protection. [Source: `fbr-portal-docs/01_PORTAL_UI/TASKLIST.md#TASK-P15`]
3. Supabase Realtime notifications include migration, RLS, provider/context/hooks, read/read-all routes, deduplication, reconnect behavior, and user isolation. [Source: `fbr-portal-docs/01_PORTAL_UI/TASKLIST.md#TASK-P16`]
4. Notification UI includes toast stack, error-toast persistence, right drawer, module filter, read-all action, and sidebar badge updates. [Source: `fbr-portal-docs/01_PORTAL_UI/TASKLIST.md#TASK-P17`]
5. `postMessage` security rejects unknown origins, invalid navigation URLs, late `MODULE_READY` after timeout, and unloaded iframe errors without throwing to the user.
6. Tests cover iframe readiness/auth flow, routing guards, unauthorized access, Supabase notification RLS, read operations, toasts, drawer behavior, and sidebar badge updates.
7. Quality gates pass: lint, typecheck, tests, and build if available.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] Implement `ModuleFrame` and `useModuleFrame`. (AC: 1, 5)
  - [ ] Add iframe sandbox `allow-scripts allow-same-origin allow-forms`, skeleton, error state, retry, timeout, auth handoff, and navigation listener.
  - [ ] Handle 404/offline modules, missing `MODULE_READY`, invalid URLs, navigation before load, unloaded iframes, late ready events, and origin validation.
- [ ] Implement module routing and guards. (AC: 2)
  - [ ] Add protected layout, root redirect, 9 module pages, unauthorized page, `ModuleGuard`, and module role config.
  - [ ] Fail closed when `user_roles` lookup fails.
- [ ] Implement notifications data/realtime layer. (AC: 3)
  - [ ] Add Supabase migration, RLS, context/provider/hooks, browser client, notification types, read/read-all API routes.
  - [ ] Add reconnect/backoff and dedupe by notification ID.
- [ ] Implement notification UI. (AC: 4)
  - [ ] Add toast container/toast, drawer panel, item, filter, hook, layout provider wiring, sidebar badge integration.
- [ ] Add tests and security checks. (AC: 5, 6, 7)
  - [ ] Unit-test iframe, routing, guards, notification state, toasts, panel filtering.
  - [ ] Integration-test iframe stub `MODULE_READY`, `AUTH_TOKEN` send, route guard, read/read-all, Supabase realtime mock.
  - [ ] Security-test unknown origin `postMessage`, invalid `NAVIGATION`, RLS cross-user notification isolation.
- [ ] Update story checkboxes and File List during implementation. (AC: 7)

## Dev Notes

### Dependencies

- Requires Story 1.1 for auth/session and portal bridge.
- Requires Story 1.12 for Sidebar/Topbar/layout and base UI components.
- Admin CRUD, health, settings, and final E2E/security review are in Story 1.14.

### Source Context

- Portal UI SPEC Sprint 3: `fbr-portal-docs/01_PORTAL_UI/SPEC.md#sprint-3-embedding-de-modulos-notificacoes`.
- Detailed executable tasks: `fbr-portal-docs/01_PORTAL_UI/TASKLIST.md#TASK-P14` through `TASK-P17`.
- Embedding contract: `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md#3.4-micro-frontends-isolamento-e-integracao`.
- Notification payload pattern: `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md#13-padroes-de-componentes-ui` and Portal UI tasklist.

### File Locations

- `apps/portal/src/components/ModuleFrame/*`
- `apps/portal/src/hooks/useModuleFrame.ts`
- `apps/portal/src/config/modules.ts`
- `apps/portal/src/app/(portal)/layout.tsx`
- `apps/portal/src/app/(portal)/page.tsx`
- `apps/portal/src/app/(portal)/{click,finance,leads,mkt,redacao,sales,social,videoflow,design}/page.tsx`
- `apps/portal/src/app/unauthorized/page.tsx`
- `apps/portal/src/components/ModuleGuard.tsx`
- `supabase/migrations/002_create_notifications.sql`
- `apps/portal/src/contexts/NotificationsContext.tsx`
- `apps/portal/src/contexts/NotificationsProvider.tsx`
- `apps/portal/src/hooks/useNotifications.ts`
- `apps/portal/src/hooks/useToast.ts`
- `apps/portal/src/app/api/notifications/read/route.ts`
- `apps/portal/src/app/api/notifications/read-all/route.ts`
- `apps/portal/src/lib/supabase-client.ts`
- `apps/portal/src/types/notifications.ts`
- `apps/portal/src/components/Notifications/*`

### Testing

- ModuleFrame: sandbox attr, skeleton, timeout, retry, auth send on ready, URL sync, invalid URL ignored, late ready no duplicate auth send.
- ModuleGuard: authorized children, unauthorized page, root redirect by role, fail closed on DB error.
- Notifications: RLS user isolation, realtime subscribe, dedupe, reconnect, read idempotency, read-all.
- UI: toast auto-dismiss for info, no auto-dismiss for error, max 5 visible toasts, drawer filtering, EmptyState, immediate list update while open, badge update.
- Security: unknown origin message ignored, fake `postMessage` ignored, no user data persisted to localStorage by module embedding flow.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-02 | 0.1 | Split from original Story 1.1 after PO NO-GO | @sm |

## Dev Agent Record

### Agent Model Used

TBD

### Debug Log References

TBD

### Completion Notes List

TBD

### File List

TBD

## QA Results

### PO Story Draft Validation - 2026-05-04

Decision: NO-GO
Readiness Score: 6/10
Confidence: Medium

Checklist Result:

| Category | Status | Issues |
|---|---|---|
| Goal & Context Clarity | PASS | Story goal and business value are clear. Dependencies are listed. |
| Technical Implementation Guidance | PARTIAL | Key files and flows are listed, but executor assignment is weak for mixed frontend + DB/RLS work. |
| Reference Effectiveness | PASS | References point to TASK-P14..P17, SPEC Sprint 3, embedding contract, and UI patterns. |
| Self-Containment Assessment | PARTIAL | Core behavior is present, but cross-story dependency status blocks readiness. |
| Testing Guidance | PASS | Unit, integration, realtime, RLS, UI, and postMessage security scenarios are specified. |
| CodeRabbit Integration | N/A | Disabled in `core-config.yaml`; skip notice is present. |

Blocking Issues:

1. Story 1.13 depends on Story 1.12 for Sidebar/Topbar/layout and base UI components, but `docs/stories/1.12.portal-ui-shell-navigation.md` is still `Draft`. Per Constitution Article III, this story cannot move to Ready until the dependency is validated/ready or the dependency risk is explicitly resolved in the story lifecycle.
2. Executor assignment is not fully aligned with the validation matrix for mixed DB/RLS work. The story includes `supabase/migrations/002_create_notifications.sql`, RLS, notification API routes, and frontend embedding/UI. Current `executor: "@dev"` / `quality_gate: "@architect"` covers code/features, but the DB/RLS portion maps to `@data-engineer` with `@dev` quality gate in the executor validation table. This needs either a documented split/delegation or revised assignment before implementation.

Should-Fix Issues:

1. AC2 inherits `/` redirect behavior from TASK-P15 including the `super_admin -> /admin/users` example, while the story also says Admin CRUD is in Story 1.14. Clarify expected behavior if `/admin/users` is not implemented yet, without expanding Story 1.13 scope.
2. Add explicit Definition of Done / scope boundary in the story body for what is included in 1.13 versus deferred to 1.14, especially admin routes, final E2E/security review, and broader RLS schema work.
3. Quality gates list includes lint/typecheck/test/security-review, while AC7 says build if available. Add `build` to `quality_gate_tools` or document why build is not required for this draft.

Anti-Hallucination Findings:

- PASS: AC1-AC4 trace to `fbr-portal-docs/01_PORTAL_UI/TASKLIST.md#TASK-P14` through `#TASK-P17`.
- PASS: Embedding contract traces to `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md#3.4-micro-frontends-isolamento-e-integracao`.
- PASS: Notification table/realtime/RLS traces to `fbr-portal-docs/01_PORTAL_UI/SPEC.md#sprint-3-embedding-de-modulos-notificacoes`.
- INFO: Code intelligence duplicate check auto-skipped because no provider was initialized/available through the project helper.

Gate Execution:

- Draft validation checklist: Executed manually by @po.
- `npm run lint`: Not run before NO-GO; blocked by draft readiness/dependency issue.
- `npm run typecheck`: Not run before NO-GO; blocked by draft readiness/dependency issue.
- `npm test`: Not run before NO-GO; blocked by draft readiness/dependency issue.
- `npm run build`: Not run before NO-GO; blocked by draft readiness/dependency issue and not listed in `quality_gate_tools`.

Final Assessment:

Story requires revision before implementation. Keep status as `Draft`.
