# Story 1.4.2: Finance Domain Model, RLS, and Audit Invariants

## Status

Draft

## Executor Assignment

executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools:
- story-draft-checklist
- migration-review
- lint
- typecheck
- test

## Story

**As a** finance manager,
**I want** Finance records modeled with company isolation, approval structures, and immutable audit history,
**so that** all financial data can be traced and governed across group companies.

## Acceptance Criteria

1. Migration coverage exists for all 14 Finance tables: `companies`, `cost_centers`, `transactions`, `receivables`, `payables`, `bank_accounts`, `bank_statements`, `reconciliation_items`, `budgets`, `suppliers`, `approval_flows`, `forecasts`, `audit_log`, and `agent_actions`.
2. SPEC baseline tables/semantics for `empresas_financeiras`, `recebimentos`, `pagamentos`, and `audit_financeiro` are either represented directly or explicitly mapped to the English table names.
3. Finance tables enforce company isolation/RLS so users from company A cannot read or mutate company B records.
4. Audit records are append-only; direct update/delete attempts fail outside approved trigger/service boundaries.
5. Receivables/payables mutations create audit records preserving `valor_antes` and `valor_depois`.
6. SQL-backed monetary values use `NUMERIC(15,2)`, timestamps use `TIMESTAMPTZ`, before/after payloads use `JSONB`, and identifiers use UUIDs.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] Define Finance data model and migration layout. (AC: 1, 2, 6)
  - [ ] Create or extend the repo-approved migration surface for the 14 required tables.
  - [ ] Document any Portuguese SPEC baseline to English table-name mapping in migration comments or Dev Notes.
  - [ ] Add constraints for UUID identifiers, positive monetary values, FK relationships, and timestamp fields.
- [ ] Implement RLS/company isolation. (AC: 3)
  - [ ] Enable company isolation policies on Finance tables.
  - [ ] Ensure read and mutation paths are company-scoped.
- [ ] Implement audit invariants. (AC: 4, 5)
  - [ ] Make audit records append-only.
  - [ ] Add triggers or service-controlled audit writes for receivables/payables mutations.
  - [ ] Preserve before/after JSON values.
- [ ] Add database validation tests. (AC: 1-6)
  - [ ] Cover migration apply/reset, FK errors, positive money constraints, RLS isolation, audit trigger writes, audit append-only rejection, and batch audit non-duplication.
  - [ ] Run `npm run lint`, `npm run typecheck`, and `npm test`.

## Dev Notes

### Source Context

- The 14 required Finance tables come from `fbr-portal-docs/03_FINANCE/TASKS_FINANCE.md#fn-d01-expandir-tabelas-14-total`.
- SPEC Sprint 1 defines baseline Finance semantics for `empresas_financeiras`, `recebimentos`, `pagamentos`, and `audit_financeiro`, company isolation via `empresa_id`, money values as `NUMERIC(15,2)`, UUID IDs, timestamps, and audit before/after JSON. [Source: `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#sprint-1-recebimentos-e-estrutura-de-dados`]
- Audit log is append-only and records entity, entity id, actor type, actor id, action, before value, after value, and timestamp. [Source: `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#sprint-1-recebimentos-e-estrutura-de-dados`]

### File Locations

- Product docs mention Supabase migrations and backend/FastAPI surfaces, but this repo currently has no tracked `backend/` or `supabase/` directory in the parent story notes.
- If database files are created, they must be integrated into the repo workspaces/build/test flow rather than left detached.
- Shared app types may live under `apps/portal/src/lib/finance/{types,schemas,store,fixtures}.ts`, but these files do not satisfy AC 1, AC 3, AC 4, or AC 5 unless an approved executable SQL/migration runtime is also present.

### Project Structure Notes

- This story is the blocking data foundation for Stories 1.4.3 through 1.4.7.
- Current repo architecture documents only approve the Node.js/npm workspace with `apps/`, `packages/`, `docs/`, and `fbr-portal-docs`; they do not approve a concrete database migration/RLS runtime.
- Because executable migrations, RLS policies, triggers, and SQL-backed invariant tests are architecture/runtime placement concerns, @sm cannot resolve the remaining PO blocker without an @architect decision.

### Architecture Decision Required Before Ready

**Decision owner:** `@architect`

**Decision needed:** choose and document the executable database migration/RLS runtime for Finance Story 1.4.2.

The decision must state, at minimum:

1. The approved path/package for migration SQL files.
2. The command(s) that apply and reset migrations locally or in CI.
3. The test harness path/package that verifies migration apply/reset, RLS isolation, audit triggers, append-only rejection, FK errors, positive-money constraints, and batch audit non-duplication.
4. How the migration/test package is included in the existing workspace gates: `npm run lint`, `npm run typecheck`, and `npm test`.
5. Whether source-doc references to `supabase/migrations` and `backend/api` are adopted into this repo, mapped to a workspace package, or explicitly rejected for this implementation.

**Current decision status:** missing. Keep this story `Draft` and PO `NO-GO` until the architecture decision is recorded in an architecture artifact or directly in this story by `@architect`, then re-run PO validation.

**SM rework verification (2026-05-05):** still missing after rechecking the active architecture shards (`docs/architecture/README.md`, `docs/architecture/arvore-de-origem.md`, `docs/architecture/pilha-tecnologica.md`), `.aiox-core/core-config.yaml`, root workspace directories, `apps/`, and `packages/`. Existing product/source docs and older story notes mention Supabase-style migrations, but no current architecture artifact approves `supabase/migrations`, `backend/`, or a workspace package as the executable Finance migration/RLS runtime for this repo.

**SM rework verification (2026-05-05, latest):** still missing after rechecking the active architecture shards, `package.json` workspace configuration, root directories, and implementation-directory candidates. The only matching runtime-like directory found is `.aiox-core/core/migration`, which is L1 core/framework infrastructure and not an approved Finance implementation surface. Therefore, @sm cannot resolve the PO blocker by choosing `supabase/`, `backend/`, `packages/*`, or another SQL runtime path.

### Testing

- Required tests: migration apply/reset, RLS isolation, insert/update audit trigger behavior, audit append-only rejection, FK errors, positive money constraint, and batch audit non-duplication.
- Required final gates: `npm run lint`, `npm run typecheck`, and `npm test`.

## Story Draft Checklist Result

| Category | Status | Issues |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Data foundation and governance value are explicit. |
| 2. Technical Implementation Guidance | PARTIAL | Repo migration runtime needs implementation decision because backend/Supabase dirs are absent. |
| 3. Reference Effectiveness | PASS | Table, RLS, and audit references are specific. |
| 4. Self-Containment Assessment | PASS | Required tables and invariants are listed. |
| 5. Testing Guidance | PASS | Database/security tests are explicit. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled in `core-config.yaml`. |

**PO Validation Result (2026-05-05): NO-GO, 6/10.**

- Template compliance: PASS. Required sections are present; optional Community Origin is not applicable.
- Executor assignment: PASS. Schema/DB/RLS/Migrations work maps to `@data-engineer` with `@dev` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs trace to `TASKS_FINANCE.md` and `PRD_SPEC_TASKLIST.md` without adding unsupported Finance requirements.
- CodeRabbit: N/A. Skip notice is present and `.aiox-core/core-config.yaml` has no enabled CodeRabbit integration.
- Blocking issue: migration/RLS execution surface is not concrete enough for implementation. The repo has `apps/`, `packages/`, and docs, but no tracked `backend/` or `supabase/`; the story must state the approved executable migration package/path and how it is wired into `npm run lint`, `npm run typecheck`, and `npm test`.
- Revalidation note: the blocking issue still stands after checking `.aiox-core/core-config.yaml`, `docs/architecture/arvore-de-origem.md`, `docs/architecture/pilha-tecnologica.md`, repo directories, and the Finance source docs. No architecture artifact currently approves a concrete migration/RLS runtime path.

**Final Assessment:** NEEDS REWORK before Ready. Architecture/runtime placement must be resolved in-story before implementation starts.

**SM Rework Result (2026-05-05): Draft/NO-GO maintained.**

- The PO blocking issue was decomposed into a concrete architecture decision request in `Architecture Decision Required Before Ready`.
- No migration path/package was invented by @sm because current architecture artifacts do not approve `backend/`, `supabase/`, or another executable database package.
- Ready condition: @architect records the migration/RLS runtime decision and @po revalidates the story.

**PO Revalidation Result (2026-05-05): NO-GO, 6/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Schema/DB/RLS/Migrations work remains assigned to `@data-engineer` with `@dev` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs continue to trace to `TASKS_FINANCE.md` and `PRD_SPEC_TASKLIST.md`; no unsupported Finance requirement was added.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.
- Blocking issue remains: architecture docs still approve only the Node.js/npm workspace (`apps/`, `packages/`, `docs/`, `fbr-portal-docs`) and do not define the executable migration/RLS runtime, SQL file path/package, apply/reset commands, or invariant test harness wiring required for implementation.

**Final Assessment:** NO-GO. Keep the story in `Draft` until `@architect` records the migration/RLS runtime decision, then rerun PO validation.

**PO Revalidation Result (2026-05-05): NO-GO, 6/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Schema/DB/RLS/Migrations work remains assigned to `@data-engineer` with `@dev` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs continue to trace to `fbr-portal-docs/03_FINANCE/TASKS_FINANCE.md#fn-d01-expandir-tabelas-14-total` and `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#sprint-1-recebimentos-e-estrutura-de-dados`.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.
- Blocking issue remains: current architecture artifacts still define only the Node.js/npm workspace (`apps/`, `packages/`, `docs/`, `fbr-portal-docs`) and do not approve a concrete executable database migration/RLS runtime. Root inspection also found no tracked `backend/`, `supabase/`, `migrations`, `database`, or `db` implementation directory outside irrelevant `.git`/`node_modules` matches.

**Final Assessment:** NO-GO. Keep the story in `Draft` until `@architect` records the migration/RLS runtime decision, then rerun PO validation.

**SM Rework Result (2026-05-05): Draft/NO-GO maintained.**

- PO blocker resolution status: unresolved by design because it requires an architecture authority decision; @sm must not choose or invent the migration/RLS runtime.
- Concrete required decision is already captured in `Architecture Decision Required Before Ready` with path/package, apply/reset commands, invariant test harness, workspace gate wiring, and Supabase/backend adoption-or-rejection requirements.
- Rechecked active architecture shards and workspace layout; no approved executable migration/RLS surface exists yet.
- Next valid lifecycle action: `@architect` records the migration/RLS runtime decision, then `@po` reruns story validation.

**PO Revalidation Result (2026-05-05): NO-GO, 6/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Schema/DB/RLS/Migrations work remains assigned to `@data-engineer` with `@dev` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs continue to trace to `fbr-portal-docs/03_FINANCE/TASKS_FINANCE.md#fn-d01-expandir-tabelas-14-total` and `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#sprint-1-recebimentos-e-estrutura-de-dados` without adding unsupported Finance requirements.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Blocking issue remains: active architecture shards still define only the Node.js/npm workspace (`apps/`, `packages/`, `docs/`, `fbr-portal-docs`) and do not approve a concrete executable database migration/RLS runtime, SQL path/package, apply/reset command, or invariant test harness.
- Workspace verification: root inspection found no tracked `backend/`, `supabase/`, `migrations`, `database`, or `db` implementation directory outside ignored/unrelated matches.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.

**Final Assessment:** NO-GO. Keep the story in `Draft` until `@architect` records the migration/RLS runtime decision, then rerun PO validation.

**PO Revalidation Result (2026-05-05): NO-GO, 6/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Schema/DB/RLS/Migrations work remains assigned to `@data-engineer` with `@dev` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs continue to trace to `fbr-portal-docs/03_FINANCE/TASKS_FINANCE.md#fn-d01-expandir-tabelas-14-total` and `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#sprint-1-recebimentos-e-estrutura-de-dados` without adding unsupported Finance requirements.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Workspace verification: root inspection still found no tracked `backend/`, `supabase/`, `migrations`, `database`, or `db` implementation directory outside ignored/dependency output, and the active workspace remains `apps/*` plus `packages/*`.
- Blocking issue remains: active architecture shards still define only the Node.js/npm workspace (`apps/`, `packages/`, `docs/`, `fbr-portal-docs`) and do not approve a concrete executable database migration/RLS runtime, SQL path/package, apply/reset command, or invariant test harness. Product/source docs mention `supabase/migrations` and `backend/api`, but those paths are not adopted or explicitly rejected by the current architecture artifacts.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd test`, and `npm.cmd run build` exited 0 during this revalidation.

**Final Assessment:** NO-GO. Keep the story in `Draft` until `@architect` records the migration/RLS runtime decision, then rerun PO validation.

**SM Rework Result (2026-05-05): Draft/NO-GO maintained.**

- PO blocker resolution status: unresolved because the missing executable migration/RLS runtime is an architecture-authority decision.
- No implementation path was invented. Current architecture remains limited to `apps/*`, `packages/*`, docs, and source corpus references; no architecture artifact adopts or rejects `supabase/migrations` or `backend/api`.
- The concrete required decision remains the `Architecture Decision Required Before Ready` section: approved SQL path/package, apply/reset commands, invariant test harness, workspace gate wiring, and source-doc path adoption/rejection.
- Next valid lifecycle action: `@architect` records the runtime decision, then `@po` reruns story validation. Until then, this story stays `Draft` and PO `NO-GO`.

**PO Revalidation Result (2026-05-05): NO-GO, 6/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Schema/DB/RLS/Migrations work remains assigned to `@data-engineer` with `@dev` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs continue to trace to `fbr-portal-docs/03_FINANCE/TASKS_FINANCE.md#fn-d01-expandir-tabelas-14-total` and `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#sprint-1-recebimentos-e-estrutura-de-dados` without adding unsupported Finance requirements.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Architecture/workspace verification: active architecture shards still define only the Node.js/npm workspace (`apps/`, `packages/`, `docs/`, `fbr-portal-docs`); root inspection found no tracked implementation `backend/`, `supabase/`, `migrations`, `database`, or `db` directory. The only matching `migration` directory remains L1 `.aiox-core/core/migration`, which is framework infrastructure and not a Finance implementation surface.
- Blocking issue remains: the story still lacks an approved executable database migration/RLS runtime, SQL path/package, apply/reset commands, invariant test harness, and gate wiring. Product/source docs mention Supabase/backend paths, but current architecture artifacts do not adopt or explicitly reject them for this repo.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.

**Final Assessment:** NO-GO. Keep the story in `Draft` until `@architect` records the migration/RLS runtime decision, then rerun PO validation.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-03 | 0.1 | Split from parent Story 1.4 Finance backbone scope | @sm |
| 2026-05-04 | 0.2 | PO validation completed; story remains Draft/NO-GO pending executable migration surface decision | @po |
| 2026-05-05 | 0.3 | PO revalidation completed; NO-GO reaffirmed and gates rerun successfully | @po |
| 2026-05-05 | 0.4 | SM rework recorded concrete architecture decision needed for migration/RLS runtime; story remains Draft/NO-GO | @sm |
| 2026-05-05 | 0.5 | PO revalidation completed after SM rework; NO-GO maintained pending @architect migration/RLS runtime decision | @po |
| 2026-05-05 | 0.6 | PO revalidation rerun; NO-GO maintained and required gates passed | @po |
| 2026-05-05 | 0.7 | SM rework rerun; PO blocker confirmed as architecture-authority dependency and story kept Draft/NO-GO | @sm |
| 2026-05-05 | 0.8 | PO revalidation rerun; NO-GO maintained pending @architect migration/RLS runtime decision | @po |
| 2026-05-05 | 0.9 | PO revalidation rerun; NO-GO maintained, workspace/architecture blocker reconfirmed, and required gates/build passed | @po |
| 2026-05-05 | 1.0 | SM rework rerun; architecture decision dependency reconfirmed without inventing migration/RLS runtime, story kept Draft/NO-GO | @sm |
| 2026-05-05 | 1.1 | PO revalidation rerun; NO-GO maintained, architecture/runtime blocker reconfirmed, and required gates passed | @po |

## Dev Agent Record

### Agent Model Used

Codex GPT-5

### Debug Log References

- 2026-05-05: Read `.aiox-core/constitution.md`, `.aiox-core/development/agents/sm.md`, `docs/architecture/arvore-de-origem.md`, `docs/architecture/pilha-tecnologica.md`, and Finance source docs.
- 2026-05-05: Inspected root workspace/package layout and confirmed no tracked `backend/` or `supabase/` directory.
- 2026-05-05: Read `.aiox-core/development/agents/po.md`, `.aiox-core/development/tasks/validate-next-story.md`, `.aiox-core/core-config.yaml`, `.aiox-core/product/templates/story-tmpl.yaml`, Finance source docs, and architecture docs for PO revalidation.
- 2026-05-05: Reran PO validation for `*validate-story-draft`, rechecked architecture/source docs, root implementation directories, CodeRabbit config, and required quality gates.
- 2026-05-05: Reran SM rework for `*rework-story`, rechecked active architecture shards, `.aiox-core/core-config.yaml`, root workspace directories, `apps/`, and `packages/`; confirmed no approved executable Finance migration/RLS runtime exists.
- 2026-05-05: Reran PO validation for `*validate-story-draft`, rechecked Constitution, PO agent workflow, story template/checklist, core config, active architecture shards, Finance source docs, workspace layout, and implementation-directory absence.
- 2026-05-05: Reran PO validation for `*validate-story-draft`, rechecked Constitution, PO task workflow, story template/checklist, core config, active architecture shards, Finance source docs, root/workspace layout, and absence of an approved executable Finance migration/RLS runtime.
- 2026-05-05: Reran SM rework for `*rework-story`, rechecked AIOS Constitution, SM agent authority, active architecture shards, `package.json` workspace configuration, root workspace layout, and implementation-directory candidates; confirmed the only `migration` directory found is L1 `.aiox-core/core/migration`, not a Finance runtime.
- 2026-05-05: Reran PO validation for `*validate-story-draft`, rechecked AIOS Constitution, PO task workflow, story template/checklist, `.aiox-core/core-config.yaml`, active architecture shards, `package.json` workspace configuration, root/workspace layout, Finance source docs, and implementation-directory candidates.

### Completion Notes List

- 2026-05-04 PO validation only; no implementation performed.
- 2026-05-05 PO revalidation only; no implementation performed.
- Story remains Draft/NO-GO because executable migration/RLS placement is unresolved.
- 2026-05-05 SM rework: PO blocker converted into a concrete @architect decision request without inventing a migration runtime.
- Gates run during PO validation: `npm run lint`, `npm run typecheck`, and `npm test` all exited 0. PowerShell emitted a non-blocking `npm.ps1` permission warning while checking the global npm CLI path.
- Gates run during 2026-05-05 PO revalidation: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- Gates run during 2026-05-05 SM rework: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- Gates run during 2026-05-05 PO revalidation after SM rework: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- Gates run during 2026-05-05 PO revalidation rerun: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 SM rework rerun: no implementation performed; PO blocker remains dependent on @architect decision and story remains Draft/NO-GO.
- Gates run during 2026-05-05 SM rework rerun: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 PO revalidation rerun: no implementation performed; story remains Draft/NO-GO because executable Finance migration/RLS runtime is still not architecturally approved.
- Gates run during this PO revalidation: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 latest PO revalidation rerun: no implementation performed; active architecture/workspace blocker remains unresolved.
- Gates run during latest PO revalidation: `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd test`, and `npm.cmd run build` all exited 0.
- 2026-05-05 latest SM rework rerun: no implementation performed; PO blocker remains dependent on @architect runtime decision and story remains Draft/NO-GO.
- Gates run during latest SM rework rerun: `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd test`, and `npm.cmd run build` all exited 0. Build emitted the existing non-blocking Next.js warning that the Next plugin was not detected in ESLint configuration.
- 2026-05-05 PO revalidation rerun: no implementation performed; story remains Draft/NO-GO because executable Finance migration/RLS runtime is still not architecturally approved.
- Gates run during this PO revalidation: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.

### File List

- docs/stories/1.4.2.finance-domain-model-rls-audit.md

## QA Results

Not applicable yet. Story is still Draft/NO-GO and has not entered implementation/QA review.
