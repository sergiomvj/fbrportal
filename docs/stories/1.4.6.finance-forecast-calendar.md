# Story 1.4.6: Finance Forecast and Payment Calendar

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
- story-draft-checklist
- algorithm-review
- lint
- typecheck
- test

## Story

**As a** finance manager,
**I want** forecast projections and a payment calendar,
**so that** cash-flow scenarios can be reviewed across 30, 60, and 90 day horizons.

## Acceptance Criteria

1. `GET /api/proxy/finance/forecast` returns 30/60/90 day projections.
2. Forecast supports base, optimistic, and pessimistic scenarios.
3. Forecast response includes trend, R2, seasonality, insufficient-data behavior, and scenario ordering.
4. Forecast UI uses a Recharts composed chart with historical area, scenario lines, today reference line, responsive container, and BRL tooltip formatting.
5. UI includes horizon toggle, scenario checkboxes, responsive layout, calendar dots, and day detail modal.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] Implement forecast API. (AC: 1, 2, 3)
  - [ ] Add `GET /api/proxy/finance/forecast`.
  - [ ] Calculate 30/60/90 day projections, scenarios, trend, R2, and seasonality.
  - [ ] Return deterministic insufficient-data behavior and stable scenario ordering.
- [ ] Build forecast chart and payment calendar UI. (AC: 4, 5)
  - [ ] Add Recharts composed chart with historical area, scenario lines, today reference line, and BRL tooltips.
  - [ ] Add horizon toggle and scenario checkboxes.
  - [ ] Add payment calendar dots and day detail modal.
- [ ] Add focused tests. (AC: 1-5)
  - [ ] Cover forecast trend/seasonality/scenario calculations, insufficient data, scenario ordering, chart toggles, calendar rendering, modal behavior, and responsive states where supported.
  - [ ] Run `npm run lint`, `npm run typecheck`, and `npm test`.

## Dev Notes

### Source Context

- Forecast API is specified in `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#task-fi12-api-route-de-forecast-algoritmo-de-projecao-linear-com-sazonalidade`.
- Forecast chart and payment calendar UI are specified in `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#task-fi14-frontend-grafico-de-forecast-calendario-de-pagamentos`.
- Forecast UI requires Recharts composed chart with historical area, scenario lines, today reference line, responsive container, and BRL tooltip formatting.

### File Locations

- Suggested proxy route: `apps/portal/src/app/api/proxy/finance/forecast/route.ts`
- Suggested UI routes: `apps/portal/src/app/finance/forecast/page.tsx` and `apps/portal/src/app/finance/calendario/page.tsx`
- Suggested helpers: `apps/portal/src/lib/finance/{types,schemas,store,fixtures}.ts`

### Dependencies

- Depends on Story 1.4.2 for forecast storage/data model.
- Forecast inputs may depend on receivables/payables from Stories 1.4.3 and 1.4.4.

### Testing

- Required tests: forecast trend/seasonality/scenarios, R2 behavior, insufficient-data handling, scenario ordering, chart toggles, calendar dots, day detail modal, and BRL tooltip formatting.
- Required final gates: `npm run lint`, `npm run typecheck`, and `npm test`.

## Story Draft Checklist Result

| Category | Status | Issues |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Forecast/calendar value is clear. |
| 2. Technical Implementation Guidance | PARTIAL | Endpoint, algorithm outputs, and UI components are listed, but dependency/runtime readiness is blocked by earlier Finance stories and missing Recharts dependency planning. |
| 3. Reference Effectiveness | PASS | API and UI references are specific. |
| 4. Self-Containment Assessment | PARTIAL | Scenario and insufficient-data behavior are listed, but the story depends on unresolved forecast storage/data and receivables/payables contracts. |
| 5. Testing Guidance | PARTIAL | Algorithm and component tests are listed, but dependency-install and CLI/API-first verification are not explicit. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled in `core-config.yaml`. |

**Final Assessment:** NO-GO FOR IMPLEMENTATION.

## PO Validation Report

**Date:** 2026-05-04  
**Agent:** @po  
**Command:** `*validate-story-draft docs/stories/1.4.6.finance-forecast-calendar.md`  
**Decision:** NO-GO  
**Implementation Readiness Score:** 6/10  
**Confidence Level:** Medium

### Template Compliance Issues

- PASS: Required story template sections are present: Status, Executor Assignment, Story, Acceptance Criteria, CodeRabbit Integration, Tasks / Subtasks, Dev Notes, Testing, Change Log, Dev Agent Record, QA Results.
- PASS: No unfilled template placeholders were found in implementation-owned story sections. `TBD` remains acceptable in Dev Agent Record before development.
- PASS: Executor assignment is present and valid: executor `@dev`, quality gate `@architect`, non-empty quality gate tools, and executor differs from quality gate.

### Critical Issues

1. **Blocking Finance foundation not ready:** The story depends on Story 1.4.2 for forecast storage/data model, but Story 1.4.2 is still Draft/NO-GO because the executable migration/RLS placement is unresolved.
2. **Upstream input contracts not ready:** Forecast inputs may depend on receivables/payables from Stories 1.4.3 and 1.4.4. Both are still Draft/NO-GO, so the data feeding forecast/calendar behavior is not implementation-ready.
3. **Recharts dependency not planned in-story:** AC4 requires Recharts, but `apps/portal/package.json` and root `package.json` do not currently list `recharts`. The story does not specify adding the package or validating the approved dependency path, so implementation would require an unstated dependency change.
4. **CLI/API-first verification missing before UI:** The story lists API before UI, but it does not define a CLI/API-first verification path for forecast scenarios before building the chart/calendar UI, which weakens Constitution Article I compliance.

### Should-Fix Issues

- Add an explicit task/subtask to add and validate the Recharts dependency, or reference the approved existing chart dependency if one is introduced by an earlier story.
- Clarify whether Story 1.4.6 can proceed only after Stories 1.4.2, 1.4.3, and 1.4.4 are Approved/implemented, or narrow this story to fixture-backed behavior with a clear later integration story.
- Add concrete API-first validation cases: 30/60/90 horizons, scenario filter/default behavior, insufficient data response, invalid horizon normalization, and stable scenario ordering.
- Add calendar data source guidance using only source artifacts: whether calendar dots come from receivables, payables, or both, and which statuses map to expected/overdue/upcoming dots.

### Anti-Hallucination Findings

- PASS: Forecast endpoint, 30/60/90 projections, scenarios, trend, R2, seasonality, insufficient-data behavior, and scenario ordering trace to `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md` TASK-FI12.
- PASS: Recharts `ComposedChart`, historical area, scenario lines, today reference line, `ResponsiveContainer`, BRL tooltip formatting, horizon toggle, scenario checkboxes, calendar dots, and day modal trace to TASK-FI14.
- FAIL: The installed dependency surface for Recharts is not traceable in current `package.json` files.
- FAIL: Stable forecast input contracts are not traceable because dependent stories remain Draft/NO-GO.
- INFO: Code intelligence duplicate check was skipped because `.aiox-core/core/code-intel` returned unavailable.

### CodeRabbit Integration Findings

- N/A: CodeRabbit is disabled in `.aiox-core/core-config.yaml`; skip notice is present and no CodeRabbit checkbox gates are required. Manual review fallback applies.

### Gate Execution

- PASS: `npm run lint`
- PASS: `npm run typecheck`
- PASS: `npm test`
- INFO: All three commands returned exit code 0. PowerShell emitted a non-blocking `npm.ps1` permission warning while checking global npm CLI path outside the workspace.

### Final Assessment

**NO-GO:** Story requires refinement before implementation. Required next action: unblock or explicitly isolate dependencies on Stories 1.4.2, 1.4.3, and 1.4.4; add Recharts dependency guidance; and add CLI/API-first forecast validation before UI work.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-03 | 0.1 | Split from parent Story 1.4 Finance backbone scope | @sm |
| 2026-05-04 | 0.2 | PO validation marked NO-GO pending upstream Finance dependencies, Recharts dependency guidance, and CLI/API-first validation | @po |

## Dev Agent Record

### Agent Model Used

TBD

### Debug Log References

TBD

### Completion Notes List

- 2026-05-04 PO validation only; no implementation performed.
- Story remains Draft/NO-GO because upstream Finance dependencies, Recharts dependency planning, and CLI/API-first validation are unresolved.
- Gates run during PO validation: `npm run lint`, `npm run typecheck`, and `npm test` all exited 0. PowerShell emitted a non-blocking `npm.ps1` permission warning while checking global npm CLI path.

### File List

- docs/stories/1.4.6.finance-forecast-calendar.md

## QA Results

Not applicable yet. Story is still Draft and has not entered implementation/QA review.
