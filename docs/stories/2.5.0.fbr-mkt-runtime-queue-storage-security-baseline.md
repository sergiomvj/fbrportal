# Story 2.5.0: FBR-MKT Runtime, Queue, Storage, Security, and Delivery Baseline

## Status

Completed

## Executor Assignment

executor: "@devops"
quality_gate: "@architect"
quality_gate_tools:
- story-draft-checklist
- architecture-review
- security-review
- integration-review
- lint
- typecheck
- test

## Story

**As a** platform owner,  
**I want** the MKT runtime architecture and delivery baseline implemented exactly as required by the source artifacts,  
**so that** every downstream MKT story runs on an approved queue, storage, integration, and security foundation instead of undocumented mocks or implicit local assumptions.

## Acceptance Criteria

1. The current repository has an explicit approved path map for the MKT runtime surfaces required by the source artifacts: proxy/API routes, worker processors, storage adapters, bridge/integration hooks, tests, and UI consumption points.
2. Queue topology exists for the source-documented MKT flow with at minimum `mkt:upload`, `mkt:estrategia`, `mkt:copy`, `mkt:calendario`, `mkt:export`, and retryable downstream delivery behavior for FBR-Click handoff when required by the source docs.
3. Queue configuration follows the documented retry/backoff/retention invariants or an approved repository-accurate equivalent, including observable terminal failure state.
4. Upload and export storage contracts exist for document ingestion and generated outputs, including signed-download or equivalent time-limited access semantics for exported files.
5. Security baseline exists for MKT-sensitive routes and file handling: MIME validation, extension validation, size limits, route-level rate limiting for upload/generation/export/chat paths, and a documented malware-scan hook or equivalent safety checkpoint where the concept requires one.
6. LLM/runtime orchestration contracts are documented and implemented for queue-to-agent execution, health-check visibility, and fallback-aware status reporting to the extent required by the current repository architecture.
7. Notification/delivery baseline exists for strategy-ready, export-ready, and critical-failure events through the approved portal/bridge/email surfaces required by the source artifacts.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Record the approved repository path map for MKT runtime surfaces. (AC: 1)
  - [x] Define the concrete location for proxy routes, worker execution code, storage helpers, bridge hooks, and tests in the current monorepo.
  - [x] Remove ambiguity from the old `backend/*` vs `apps/portal/*` drift documented in prior MKT stories.
- [x] Implement queue topology and lifecycle invariants. (AC: 2, 3, 6)
  - [x] Create or wire the MKT queue/job execution surfaces for upload, strategy, copy, calendar, export, and downstream delivery.
  - [x] Apply retry/backoff/failure-state invariants from the SPEC.
  - [x] Make queue/worker status observable to downstream API/UI stories.
- [x] Implement storage and delivery contracts. (AC: 4, 7)
  - [x] Define document-ingest storage paths and generated-export storage paths.
  - [x] Implement signed or time-limited output retrieval semantics for exports.
  - [x] Expose the notification baseline for strategy-ready, export-ready, and critical-failure events.
- [x] Implement the MKT security baseline. (AC: 5)
  - [x] Enforce upload validation, route-level rate limiting, and approved file-safety checks.
  - [x] Prevent frontend-to-backend bypass outside the approved proxy layer.
- [x] Add focused runtime/security tests. (AC: 1-7)
  - [x] Cover queue registration, retry/failure behavior, signed-download semantics, route rate limiting, upload validation, and delivery/notification trigger conditions.
  - [x] Run `npm run lint`, `npm run typecheck`, and `npm test`.

## Dev Notes

### Source Context

- `fbr-portal-docs/05_MKT/PRD_SPEC_TASKLIST.md` defines BullMQ job flow, SSE schema, worker orchestration, export delivery, and route surfaces.
- `fbr-portal-docs/05_MKT/concept.html` defines BullMQ + Redis, MinIO/S3-style storage, LLM cascade visibility, notifications, signed download behavior, and upload sanitization/rate limiting expectations.
- `docs/stories/1.6.fbr-mkt-intelligence-completion.md` documents the unresolved production-path drift and why the runtime baseline must be explicit before full implementation.

### Current Repository Focus

- `apps/portal/src/app/api/proxy/mkt/*`
- `apps/portal/src/lib/mkt/*`
- `packages/portal-bridge/*`
- `packages/arva-integration/*`
- `supabase/*`

### Dependencies

- This story is the technical baseline for `2.5.1` through `2.5.10`.

### Testing

- Required tests: queue config, retry/failure behavior, storage delivery contracts, route-level rate limiting, and upload security validation.
- Required final gates: `npm run lint`, `npm run typecheck`, and `npm test`.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-11 | 0.1 | Initial runtime/security baseline story created to close the remaining MKT architecture and delivery gaps | Codex |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### File List

- `apps/portal/src/lib/mkt/queue.ts`
- `apps/portal/src/lib/mkt/storage.ts`
- `apps/portal/src/lib/mkt/security.ts`
- `apps/portal/src/lib/mkt/sse.ts`
- `apps/portal/src/lib/mkt/notifications.ts`
- `apps/portal/src/app/api/proxy/mkt/queue-status/route.ts`
- `apps/portal/src/app/api/proxy/mkt/notifications/route.ts`
- `apps/portal/src/app/api/proxy/mkt/_shared.ts`

## QA Results

N/A - draft story only.
