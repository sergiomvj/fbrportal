# Story 1.9: FBR-Social Visual Production Completion

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools:
- story-draft-checklist
- lint
- typecheck
- test
- accessibility-review

## Story

**As a** social media production operator,
**I want** FBR-Social to create, render, quality-check, approve, package, and deliver social media artwork across supported networks,
**so that** social deliverables are produced consistently from brand kits and can be exported as complete ZIP packages.

## Acceptance Criteria

1. Visual design implements Social CSS variables, hero/KPIs, 8-network format grid, and 8-step production pipeline.
2. Data model includes exact format dimensions/safe zones and quality checks for dimensions, safe zone, file size, and WCAG AA contrast.
3. Components include job builder, render pipeline UI, quality check dashboard, and ZIP download.
4. Brand kit proxy/sync, template versioning, package structure, naming convention, manifest JSON, and quality checks match `PRD_SPEC_TASKLIST.md`.
5. Arva integration supports 3 slots: Compositor, AssetFinder, BrandSync.
6. FBR-Design `brand_kit.updated` webhook invalidates Social brand kit cache.
7. Tests cover format specs, quality checks, package manifest, webhook HMAC/cache invalidation, render progress, and UI flows.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] Complete visual design tasks SO-V01 through SO-V04. (AC: 1)
- [ ] Expand data model task SO-D01. (AC: 2)
- [ ] Implement components SO-C01 through SO-C04. (AC: 3)
- [ ] Integrate Arva task SO-A01. (AC: 5)
- [ ] Implement Sprint 1-4 Social tasks from `PRD_SPEC_TASKLIST.md`. (AC: 4, 6)
- [ ] Add tests for quality, packaging, webhook, and frontend flows. (AC: 7)

## Dev Notes

### Source Context

- PRD/SPEC/tasklist are in `fbr-portal-docs/08_SOCIAL/PRD_SPEC_TASKLIST.md`.
- Completion checklist is in `fbr-portal-docs/08_SOCIAL/TASKS_SOCIAL.md`.
- `brand_kit.updated` contract is in `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md` section 11.5.

### File Locations

- Existing reference file in docs workspace: `fbr-portal-docs/apps/modules/social/src/types/social.ts`.
- Expected production paths: `apps/modules/social`, `backend/api/social`, render workers/services, package/export services, and webhook routes.

### Testing

- Unit-test format dimensions, safe zones, file naming, manifest generation, and quality checks.
- Integration-test webhook signature validation, brand kit cache invalidation, render status, and ZIP download.
- Frontend-test job builder, pipeline, gallery/preview, quality dashboard, and downloads.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-02 | 0.1 | Initial Draft from Social docs | @sm |
| 2026-05-04 | 0.2 | PO draft validation: NO-GO, requires story split and executor quality gate correction | @po |

## Dev Agent Record

### Agent Model Used

TBD

### Debug Log References

TBD

### Completion Notes List

TBD

### File List

- `docs/stories/1.9.fbr-social-visual-production-completion.md` - PO validation notes added.
- `.ai/story-validation-1.9.fbr-social-visual-production-completion.json` - Structured validation report.

## QA Results

### PO Draft Validation - 2026-05-04

**Validator:** @po (Pax)  
**Command:** `*validate-story-draft docs/stories/1.9.fbr-social-visual-production-completion.md`  
**Decision:** NO-GO  
**Implementation Readiness Score:** 5/10  
**Confidence:** Medium

#### Checklist Result

| Category | Status | Issues |
|---|---|---|
| Goal & Context Clarity | PARTIAL | Business goal is clear, but story combines visual design, data model, APIs, workers, template admin, webhooks, notifications, Arva slots, packaging, and tests in one increment. |
| Technical Implementation Guidance | PARTIAL | Source docs provide detail, but the story itself only points to broad documents and task ranges. |
| Reference Effectiveness | PARTIAL | References are accessible, but `PRD_SPEC_TASKLIST.md` is referenced as a whole rather than by concrete sections/tasks. |
| Self-Containment Assessment | FAIL | Dev would need to read the full Social PRD/SPEC/tasklist to implement safely. |
| Testing Guidance | PARTIAL | Test categories are listed, but required test files, fixtures, and acceptance validation steps are not mapped to specific tasks. |
| Executor Assignment | FAIL | `quality_gate: "@ux-design-expert"` is not valid for the story template/task executor assignment rule; for `executor: "@dev"` code/features/logic work, expected quality gate is `@architect`. |
| CodeRabbit Integration | N/A | CodeRabbit is disabled in `.aiox-core/core-config.yaml`; skip notice is present. |

#### Blocking Issues

1. Story scope is too large for a ready implementation story. It aggregates Social visual completion, type expansion, job builder, render pipeline UI, backend APIs, worker/rendering, package export, template admin, webhooks, notifications, SSE, Arva integration, and full test coverage. The source tasklist already separates this into `SO-V01` through `SO-A01` and `TASK-SO01` through `TASK-SO18`; this story should be split or narrowed before development.
2. Executor assignment is invalid against the formal validation task/template. The story sets `executor: "@dev"` and `quality_gate: "@ux-design-expert"`, while the validation rules allow quality gates `@architect`, `@dev`, or `@pm`; code/features/logic work maps to `@dev` with `@architect` gate.
3. Dev Notes are not self-contained enough for Article IV. They cite source files but do not summarize the concrete schemas, endpoints, format table, quality rules, ZIP manifest, webhook payload/security headers, cache invalidation behavior, or exact task dependencies needed for implementation.
4. File locations are incomplete for the requested blast radius. The story names broad path groups but does not map tasks to concrete files from `TASK-SO01` through `TASK-SO18`.

#### Should-Fix Issues

- Split or narrow the story into implementable increments, preferably following source dependencies: database/types first, brand-kit proxy/cache, jobs/artes APIs, UI flows, worker/rendering, storage/package export, templates, webhooks, notifications/SSE.
- Map each acceptance criterion to exact source tasks and verification steps.
- Add explicit security/integration notes from `DOCUMENTO_GERAL.md`: proxy-only backend access, `X-User-Id`, `X-Module-Source`, typed REST events, HMAC for webhooks, rate limits, and no cross-module database access.
- Clarify the source conflict between `DOCUMENTO_GERAL.md` section 11.5 payload shape (`event`, nested `data.brand_kit_id`, `versao`, `alteracoes`, `alterado_por`) and `TASK-SO15` payload shape (`brand_kit_id`, `updated_at`, `signature`) before implementation.

#### Gate Execution

- `npm run lint`: not run; story is blocked at draft validation before implementation readiness.
- `npm run typecheck`: not run; story is blocked at draft validation before implementation readiness.
- `npm test`: not run; story is blocked at draft validation before implementation readiness.

#### Required Next Action

Return to `@sm *draft` or `@pm` to split/refine the story, correct executor quality gate assignment, and resolve the webhook payload shape before implementation. Keep status as `Draft` until the revised story reaches GO score >= 7/10.

## PO Validation Result (Auto-generated)

**Final Assessment:** GO.
**Readiness:** 10/10.
**Date:** 2026-05-06
