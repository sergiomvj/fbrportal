# Story 1.4.4: Finance Payables, Approvals, Cost Centers, P&L, and Financial Events

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
- story-draft-checklist
- api-contract-review
- lint
- typecheck
- test

## Story

**As a** finance manager,
**I want** payables, approvals, cost centers, P&L, and cross-module financial events,
**so that** expenses are governed, allocated, and visible by company and cost center.

## Acceptance Criteria

1. `GET /api/proxy/finance/pagamentos`, `POST /api/proxy/finance/pagamentos`, and `POST /api/proxy/finance/pagamentos/{id}/aprovar` exist and register audit events.
2. Approval workflow enforces no self-approval, no skipping levels, rejection requires observation, already approved returns 409, and recurring payments schedule the next payment through the documented automation boundary.
3. Approval thresholds/role mapping from TASK-FI06 are implemented without silently merging conflicting visual thresholds from TASKS_FINANCE.
4. `GET /api/proxy/finance/centros-custo` returns a tree up to 3 levels and current-month spend from approved payments.
5. `GET /api/proxy/finance/pl/{empresa_id}` returns revenue, expenses, profit, 6-month history, and budget variance.
6. Generic `financial.event` intake creates a pending payable linked to the correct cost center.
7. Frontend includes payables table/approval workflow plus cost center tree and P&L table.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] Implement payables and approval proxy contracts. (AC: 1, 2, 3)
  - [ ] Add list/create/approve routes with audit registration.
  - [ ] Enforce role/threshold rules, no self-approval, no skipped levels, rejection observation, and 409 for already approved items.
  - [ ] Keep visual governance thresholds separate from executable approval policy when the sources conflict.
- [ ] Implement cost center and P&L APIs. (AC: 4, 5)
  - [ ] Return a cost center tree up to 3 levels.
  - [ ] Calculate current-month approved payment spend.
  - [ ] Return revenue, expenses, profit, 6-month history, and budget variance by company.
- [ ] Implement generic financial event intake. (AC: 6)
  - [ ] Accept documented `financial.event` payload fields.
  - [ ] Create pending payables linked to the correct cost center.
- [ ] Build payables, approvals, cost center, and P&L UI. (AC: 7)
  - [ ] Add payables table with role-aware approve button, confirm/reject dialogs, spinner/loading, optimistic update/rollback, recurring indicator, and toasts.
  - [ ] Add cost center tree and P&L table for hierarchical spend and monthly budget comparison.
- [ ] Add focused tests. (AC: 1-7)
  - [ ] Cover approval edge cases, audit registration, cost center tree depth/spend, P&L calculations, financial event intake, and UI workflows.
  - [ ] Run `npm run lint`, `npm run typecheck`, and `npm test`.

## Dev Notes

### Source Context

- Payables and approval routes are specified in `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#task-fi06-api-routes-de-pagamentos-e-aprovacao-com-registro-em-audit`.
- Cost center and P&L routes are specified in `fbr-portal-docs/03_FINANCE/PRD_SPEC_TASKLIST.md#task-fi07-api-route-de-centros-de-custo-e-pl-por-empresa`.
- Payables UI is specified in TASK-FI08; cost center/P&L UI is specified in TASK-FI09.
- `financial.event` from any module carries cost type, description, amount, company id, optional cost center, recurrence flag, reference date, and source module. [Source: `fbr-portal-docs/00_GERAL/DOCUMENTO_GERAL.md#117-evento-financialevent-qualquer-modulo-fbr-finance`]

### API and Component Notes

- Cross-module communication must use REST event payloads through module APIs; modules must not write directly to another module database.
- Confirmation flows should use ConfirmDialog semantics for approval/rejection/destructive decisions with loading state.
- Status badges should use documented shared semantics, with module-specific accent awareness.

### File Locations

- Suggested proxy routes: `apps/portal/src/app/api/proxy/finance/pagamentos/**/route.ts`, `apps/portal/src/app/api/proxy/finance/centros-custo/route.ts`, and `apps/portal/src/app/api/proxy/finance/pl/[empresa_id]/route.ts`
- Suggested UI routes: `apps/portal/src/app/finance/pagamentos/page.tsx`, `apps/portal/src/app/finance/centros-custo/page.tsx`, and `apps/portal/src/app/finance/pl/page.tsx`
- Suggested shared helpers: `apps/portal/src/lib/finance/{types,schemas,store,fixtures}.ts`

### Dependencies

- Depends on Story 1.4.2 for payables, cost centers, approval flows, audit log, and company isolation.

### Project Structure Notes

- Governance threshold conflict: visual section says `<=R$500 gestor`, `R$500-5k diretor`, `>5k C-level`, while TASK-FI06 specifies approval logic around `<=R$5.000` and `>R$50.000`. Do not silently merge these; request PO/architect clarification if executable policy is ambiguous.

### Architecture Decision Required Before Ready

**Decision owner:** `@architect`

**Decision needed:** resolve the executable database migration/RLS runtime decision already blocking Story 1.4.2.

Story 1.4.4 must remain `Draft`/`NO-GO` until Story 1.4.2 records, and PO approves, the concrete migration/RLS runtime for Finance. This inherited decision must define the approved SQL/migration package or path, local/CI apply-reset commands, invariant test harness, and workspace gate wiring for `npm run lint`, `npm run typecheck`, and `npm test`.

**Why this blocks 1.4.4:** payables, cost centers, approval flows, audit registration, company isolation, current-month approved spend, P&L calculations, and `financial.event` payable creation all depend on the executable Finance data/RLS/audit foundation from Story 1.4.2.

**Current decision status:** missing in Story 1.4.2 and not resolved by this story. Do not start implementation of 1.4.4 until Story 1.4.2 is approved or the `@architect` decision is recorded and PO revalidation passes.

### Testing

- Required tests: approval roles/levels/self-approval, rejection observation, already-approved conflict, recurring scheduling boundary, P&L calculations, financial event intake, and UI approval dialogs.
- Required final gates: `npm run lint`, `npm run typecheck`, and `npm test`.

## Story Draft Checklist Result

| Category | Status | Issues |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Expense governance and allocation goal is clear. |
| 2. Technical Implementation Guidance | PARTIAL | API routes, UI flows, edge cases, and threshold conflict are explicit, but implementation depends on Story 1.4.2 data/RLS/audit surface that is still Draft/NO-GO. |
| 3. Reference Effectiveness | PASS | Route/event references are specific. |
| 4. Self-Containment Assessment | PASS | Approval edge cases are included. |
| 5. Testing Guidance | PASS | API, policy, and UI tests are listed. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled in `core-config.yaml`. |

**PO Validation Result (2026-05-04): NO-GO, 7/10.**

- Template compliance: PASS. Required sections are present; optional Community Origin is not applicable.
- Executor assignment: PASS. Code/API/UI work maps to `@dev` with `@architect` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs trace to TASK-FI06, TASK-FI07, TASK-FI08, TASK-FI09, and `financial.event` source docs without adding unsupported Finance requirements.
- Approval policy conflict handling: PASS. The story correctly blocks silent merging between TASK-FI06 executable thresholds (`<=R$5.000`, `>R$50.000`) and TASKS_FINANCE visual governance thresholds (`<=R$500`, `R$500-5k`, `>5k`).
- CodeRabbit: N/A. Skip notice is present and `core-config.yaml` has no enabled CodeRabbit integration.
- Blocking issue: Story 1.4.4 depends on Story 1.4.2, but Story 1.4.2 is still Draft/NO-GO because the executable migration/RLS placement is unresolved. Payables, cost centers, approval flows, audit registration, and company isolation cannot be implementation-ready until that foundation is approved.

**Final Assessment:** NEEDS REWORK before Ready. Resolve or approve Story 1.4.2 first, then revalidate 1.4.4.

**PO Revalidation Result (2026-05-05): NO-GO, 7/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Code/API/UI work remains mapped to `@dev` with `@architect` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs continue to trace to TASK-FI06, TASK-FI07, TASK-FI08, TASK-FI09, and `financial.event` source docs without adding unsupported Finance requirements.
- Approval policy conflict handling: PASS. The story still correctly prevents silently merging TASK-FI06 executable thresholds (`<=R$5.000`, `>R$50.000`) with TASKS_FINANCE visual governance thresholds (`<=R$500`, `R$500-5k`, `>5k`).
- CodeRabbit: N/A. Skip notice is present and `.aiox-core/core-config.yaml` has no enabled `coderabbit_integration.enabled: true` setting.
- Code Intelligence Check: PASS. `validateNoDuplicates()` returned `hasDuplicates: false`.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.
- Blocking issue remains: Story 1.4.2 is still Draft/NO-GO pending an `@architect` migration/RLS runtime decision. Story 1.4.4 depends on that foundation for payables, cost centers, approval flows, audit registration, and company isolation, so implementation is not ready.

**Final Assessment:** NO-GO. Keep the story in `Draft` until Story 1.4.2 is approved or its blocking architecture decision is recorded, then rerun PO validation.

**SM Rework Result (2026-05-05): Draft/NO-GO maintained.**

- The PO blocking issue was resolved as far as `@sm` authority allows by adding `Architecture Decision Required Before Ready`.
- No migration/RLS runtime, route backing store, audit implementation, or approval persistence behavior was invented because those depend on the unresolved Story 1.4.2 architecture decision.
- Ready condition: `@architect` records the migration/RLS runtime decision for Story 1.4.2, Story 1.4.2 passes PO validation, then Story 1.4.4 is revalidated by `@po`.

**PO Revalidation Result (2026-05-05, post-SM rework): NO-GO, 7/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Code/API/UI work remains assigned to `@dev` with `@architect` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs remain traceable to TASK-FI06, TASK-FI07, TASK-FI08, TASK-FI09, and the documented `financial.event` source without adding unsupported Finance requirements.
- Approval policy conflict handling: PASS. The story continues to block silently merging TASK-FI06 executable approval thresholds with TASKS_FINANCE visual governance thresholds.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Code Intelligence Check: PASS. `validateNoDuplicates()` returned `hasDuplicates: false`.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.
- Blocking issue remains: Story 1.4.2 is still `Draft`/NO-GO because the `@architect` migration/RLS runtime decision is not recorded. Story 1.4.4 depends on that approved Finance data/RLS/audit foundation for payables, cost centers, approvals, audit registration, company isolation, current-month approved spend, P&L calculations, and `financial.event` payable creation.

**Final Assessment:** NO-GO. Keep the story in `Draft` until Story 1.4.2 records the required `@architect` decision and passes PO validation, then rerun PO validation for Story 1.4.4.

**PO Revalidation Result (2026-05-05, rerun): NO-GO, 7/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Code/API/UI work remains assigned to `@dev` with `@architect` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs remain traceable to TASK-FI06, TASK-FI07, TASK-FI08, TASK-FI09, and the documented `financial.event` source without adding unsupported Finance requirements.
- Approval policy conflict handling: PASS. The story continues to block silently merging TASK-FI06 executable approval thresholds with TASKS_FINANCE visual governance thresholds.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Code Intelligence Check: SKIPPED. `isCodeIntelAvailable()` returned `false` in this session, so duplicate validation was unavailable and non-blocking.
- Workspace/runtime check: PASS for blocker confirmation. Active architecture shards still define only the Node.js/npm workspace (`apps/`, `packages/`, `docs/`, `fbr-portal-docs`), and root inspection found no tracked executable `backend/`, `supabase/`, `migrations`, `database`, or `db` implementation directory outside ignored/unrelated paths.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.
- Blocking issue remains: Story 1.4.2 is still `Draft`/NO-GO because the `@architect` migration/RLS runtime decision is not recorded. Story 1.4.4 depends on that approved Finance data/RLS/audit foundation for payables, cost centers, approvals, audit registration, company isolation, current-month approved spend, P&L calculations, and `financial.event` payable creation.

**Final Assessment:** NO-GO. Keep the story in `Draft` until Story 1.4.2 records the required `@architect` decision and passes PO validation, then rerun PO validation for Story 1.4.4.

**SM Rework Result (2026-05-05, blocker rerun): Draft/NO-GO maintained.**

- PO blocker resolution status: unresolved by design because the remaining blocker is an `@architect` authority decision inherited from Story 1.4.2.
- Rechecked Story 1.4.2, active architecture shards, root workspace layout, `apps/`, and `packages/`; no approved executable Finance migration/RLS runtime, SQL path/package, apply/reset command, or invariant test harness is recorded.
- Concrete decision still required before Ready: `@architect` must record the migration/RLS runtime for Story 1.4.2, including SQL/migration path or package, local/CI apply-reset commands, invariant test harness, and wiring into `npm run lint`, `npm run typecheck`, and `npm test`.
- No route backing store, approval persistence behavior, audit implementation, P&L calculation persistence, or `financial.event` write behavior was invented in this story.
- Next valid lifecycle action: `@architect` records the Story 1.4.2 runtime decision, `@po` validates Story 1.4.2, then `@po` revalidates Story 1.4.4.

**PO Revalidation Result (2026-05-05, latest): NO-GO, 7/10.**

- Template compliance: PASS. Required sections remain present; optional Community Origin is not applicable.
- Executor assignment: PASS. Code/API/UI work remains assigned to `@dev` with `@architect` quality gate, and `executor != quality_gate`.
- Traceability: PASS. ACs remain traceable to TASK-FI06, TASK-FI07, TASK-FI08, TASK-FI09, and the documented `financial.event` source without adding unsupported Finance requirements.
- Approval policy conflict handling: PASS. The story still prevents silently merging TASK-FI06 executable approval thresholds with TASKS_FINANCE visual governance thresholds.
- CodeRabbit: N/A. Skip notice remains present, and `.aiox-core/core-config.yaml` still has no enabled `coderabbit_integration.enabled: true` setting.
- Code Intelligence Check: SKIPPED. `isCodeIntelAvailable()` returned `false` in this session, so duplicate validation was unavailable and non-blocking.
- Workspace/runtime check: PASS for blocker confirmation. Active architecture shards still define only the Node.js/npm workspace (`apps/`, `packages/`, `docs/`, `fbr-portal-docs`), and root inspection found no tracked executable `backend/`, `supabase/`, `migrations`, `database`, or `db` implementation directory outside ignored/unrelated paths.
- Gate status: PASS. `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` exited 0 during this revalidation.
- Blocking issue remains: Story 1.4.2 is still `Draft`/NO-GO because the `@architect` migration/RLS runtime decision is not recorded. Story 1.4.4 depends on that approved Finance data/RLS/audit foundation for payables, cost centers, approvals, audit registration, company isolation, current-month approved spend, P&L calculations, and `financial.event` payable creation.

**Final Assessment:** NO-GO. Keep the story in `Draft` until Story 1.4.2 records the required `@architect` decision and passes PO validation, then rerun PO validation for Story 1.4.4.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-03 | 0.1 | Split from parent Story 1.4 Finance backbone scope | @sm |
| 2026-05-04 | 0.2 | PO validation completed; story remains Draft/NO-GO pending Story 1.4.2 approval | @po |
| 2026-05-05 | 0.3 | PO revalidation completed; NO-GO maintained pending Story 1.4.2 approval and migration/RLS runtime decision | @po |
| 2026-05-05 | 0.4 | SM rework recorded inherited architecture decision blocker from Story 1.4.2; story remains Draft/NO-GO | @sm |
| 2026-05-05 | 0.5 | PO revalidation completed after SM rework; NO-GO maintained pending Story 1.4.2 architecture decision and approval | @po |
| 2026-05-05 | 0.6 | PO revalidation rerun; NO-GO maintained pending Story 1.4.2 architecture decision and approval | @po |
| 2026-05-05 | 0.7 | SM rework rerun; blocker confirmed as unresolved architecture-authority dependency and story kept Draft/NO-GO | @sm |
| 2026-05-05 | 0.8 | PO revalidation rerun; NO-GO maintained after gates passed and Story 1.4.2 blocker reconfirmed | @po |

## Dev Agent Record

### Agent Model Used

Codex GPT-5

### Debug Log References

- 2026-05-05: Read `.aiox-core/constitution.md`, `.aiox-core/development/agents/po.md`, `.aiox-core/development/tasks/validate-next-story.md`, `.aiox-core/core-config.yaml`, `.aiox-core/product/templates/story-tmpl.yaml`, and `.aiox-core/product/checklists/story-draft-checklist.md`.
- 2026-05-05: Rechecked `docs/stories/1.4.2.finance-domain-model-rls-audit.md`, Finance source docs, and `financial.event` source docs.
- 2026-05-05: Ran Code Intelligence duplicate check through `.aiox-core/core/code-intel/helpers/story-helper.js`.
- 2026-05-05: Read `.aiox-core/constitution.md`, `.aiox-core/development/agents/sm.md`, `docs/stories/1.4.2.finance-domain-model-rls-audit.md`, and current 1.4.4 PO blocker notes for SM rework.
- 2026-05-05: Read `.aiox-core/constitution.md`, `.aiox-core/development/agents/po.md`, `.aiox-core/development/tasks/validate-next-story.md`, `.aiox-core/product/templates/story-tmpl.yaml`, `.aiox-core/product/checklists/story-draft-checklist.md`, `.aiox-core/core-config.yaml`, `docs/stories/1.4.2.finance-domain-model-rls-audit.md`, and Finance source references for PO revalidation after SM rework.
- 2026-05-05: Ran Code Intelligence duplicate check through `.aiox-core/core/code-intel/helpers/story-helper.js`.
- 2026-05-05: Reran PO validation for `*validate-story-draft`, rechecked Constitution, PO workflow, story template/checklist, core config, Story 1.4.2 status, active architecture shards, Finance source references, workspace layout, and implementation-directory absence.
- 2026-05-05: Checked Code Intelligence availability through `.aiox-core/core/code-intel`; `isCodeIntelAvailable()` returned `false`.
- 2026-05-05: Reran SM rework for `*rework-story`, rechecked `aios-god-mode`, `.aiox-core/constitution.md`, `.aiox-core/development/agents/sm.md`, Story 1.4.2, Story 1.4.4 PO blocker notes, active architecture shards, root workspace layout, `apps/`, and `packages/`; confirmed no approved executable Finance migration/RLS runtime exists.
- 2026-05-05: Reran PO validation for `*validate-story-draft`, rechecked Constitution, PO workflow, validation task, story template/checklist, core config, Story 1.4.2, active architecture shards, Finance source references, `financial.event` source, root workspace layout, and required quality gates.
- 2026-05-05: Checked Code Intelligence availability through `.aiox-core/core/code-intel`; `isCodeIntelAvailable()` returned `false`.

### Completion Notes List

- 2026-05-04 PO validation only; no implementation performed.
- Story remains Draft/NO-GO because dependency Story 1.4.2 is still Draft/NO-GO.
- Gates run during PO validation: `npm run lint`, `npm run typecheck`, and `npm test` all exited 0. PowerShell emitted a non-blocking `npm.ps1` permission warning while checking the global npm CLI path.
- 2026-05-05 PO revalidation only; no implementation performed.
- Story remains Draft/NO-GO because Story 1.4.2 remains Draft/NO-GO pending the `@architect` migration/RLS runtime decision.
- Code Intelligence duplicate check returned no duplicate functionality.
- Gates run during 2026-05-05 PO revalidation: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 SM rework: PO blocker converted into a concrete inherited `@architect` decision request without inventing a Finance migration/RLS runtime.
- Gates run during 2026-05-05 SM rework: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 PO revalidation after SM rework: no implementation performed.
- Story remains Draft/NO-GO because Story 1.4.2 remains Draft/NO-GO pending the `@architect` migration/RLS runtime decision and PO approval.
- Code Intelligence duplicate check returned no duplicate functionality.
- Gates run during 2026-05-05 PO revalidation after SM rework: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 PO revalidation rerun: no implementation performed.
- Story remains Draft/NO-GO because Story 1.4.2 remains Draft/NO-GO pending the `@architect` migration/RLS runtime decision and PO approval.
- Code Intelligence duplicate check was unavailable in this session because `isCodeIntelAvailable()` returned `false`.
- Gates run during this PO revalidation rerun: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 SM rework rerun: no implementation performed; the PO blocker remains dependent on an `@architect` decision in Story 1.4.2, so Story 1.4.4 remains Draft/NO-GO.
- Gates run during 2026-05-05 SM rework rerun: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.
- 2026-05-05 PO revalidation rerun: no implementation performed.
- Story remains Draft/NO-GO because Story 1.4.2 remains Draft/NO-GO pending the `@architect` migration/RLS runtime decision and PO approval.
- Code Intelligence duplicate check was unavailable in this session because `isCodeIntelAvailable()` returned `false`.
- Gates run during this PO revalidation rerun: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd test` all exited 0.

### File List

- docs/stories/1.4.4.finance-payables-approvals-costcenters-pl-events.md

## QA Results

Not applicable yet. Story is still Draft/NO-GO after PO revalidation on 2026-05-05 and has not entered implementation/QA review.
